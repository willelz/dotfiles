set PATH $HOME/.nodebrew/current/bin $PATH
set PATH $HOME/neovim/bin $PATH
set PATH $HOME/.nimble/bin $PATH
alias vim="nvim"
alias la="ls -pA"

#エラー回避
function x86
  echo '(x86)'
end

function R
  echo 'R'
end
