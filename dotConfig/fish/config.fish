set PATH $HOME/.nodebrew/current/bin $PATH
set PATH $HOME/neovim/bin $PATH
set PATH $HOME/.local/bin $PATH
alias vim="nvim"
alias la="ls -pA"

set EDITOR nvim

#エラー回避
function x86
  echo '(x86)'
end

function R
  echo 'R'
end
